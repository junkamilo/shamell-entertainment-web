/**
 * `src/lib/agenda` is pure display / notification / booking-bridge helpers.
 * Empty handlers keep the events-style layout; register for consistency.
 */
export const agendaLibHandlers: never[] = [];
