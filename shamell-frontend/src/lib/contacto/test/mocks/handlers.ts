/**
 * `src/lib/contacto` is pure inquiry/availability helpers (HTTP lives in features/contacto).
 * Empty handlers keep the events-style layout; register for consistency.
 */
export const contactoLibHandlers: never[] = [];
