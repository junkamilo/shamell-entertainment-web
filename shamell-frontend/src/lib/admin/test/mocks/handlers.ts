/**
 * `src/lib/admin` is pure helpers (auth headers, routes, pricing, permissions).
 * Empty handlers keep the events-style layout; register for consistency.
 */
export const adminLibHandlers: never[] = [];
