/** Catch-all album slug — same as backend `GALLERY_CATCHALL_SLUG`; hidden from public filter tabs. */
export const GALLERY_CATCHALL_SLUG = 'gallery-all';

/** Must match backend `GALLERY_UPLOAD_MAX_FILES`. */
export const GALLERY_UPLOAD_MAX_FILES = 20;
