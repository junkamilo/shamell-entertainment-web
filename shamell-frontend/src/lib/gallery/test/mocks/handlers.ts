/**
 * `src/lib/gallery` is static fallbacks + shared constants (HTTP lives in features/gallery).
 * Empty handlers keep the events-style layout; register for consistency.
 */
export const galleryLibHandlers: never[] = [];
