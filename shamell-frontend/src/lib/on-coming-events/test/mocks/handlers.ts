/**
 * `src/lib/on-coming-events` is routes / settings / seat-label helpers.
 * HTTP for the feature lives under features/on-coming-events.
 * Empty handlers keep the events-style layout; register for consistency.
 */
export const onComingEventsLibHandlers: never[] = [];
