/**
 * `src/lib/stripe` is client helpers + branding constants (no feature HTTP).
 * Empty handlers keep the events-style layout; register for consistency.
 */
export const stripeLibHandlers: never[] = [];
