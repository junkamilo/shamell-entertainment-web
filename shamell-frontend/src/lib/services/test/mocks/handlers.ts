/**
 * `src/lib/services` is static catalog fallbacks + pure media/price helpers.
 * HTTP for admin/public services lives under features. Empty handlers keep the
 * events-style layout; register for consistency.
 */
export const servicesLibHandlers: never[] = [];
