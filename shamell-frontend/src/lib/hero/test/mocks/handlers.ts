/**
 * `src/lib/hero` is layout / preload / media-inference helpers (no feature HTTP).
 * Empty handlers keep the events-style layout; register for consistency.
 */
export const heroLibHandlers: never[] = [];
