/**
 * Root `src/lib` transversales (`nestApiErrorMessage`, `pagination`, `utils`, …)
 * are pure helpers with no feature HTTP. Empty handlers keep the events-style
 * layout; register for consistency.
 */
export const sharedLibHandlers: never[] = [];
