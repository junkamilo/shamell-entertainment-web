/**
 * Inquiries is a pure display/mapper module (no feature HTTP API).
 * Empty handlers keep the events-style test layout; register for consistency.
 */
export const inquiriesHandlers: never[] = [];
