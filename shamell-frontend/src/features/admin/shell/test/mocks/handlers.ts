/**
 * Shell has no feature-owned HTTP API (badge uses venue-reservations).
 * Empty handlers keep the events-style test layout; register for consistency.
 */
export const shellHandlers: never[] = [];
