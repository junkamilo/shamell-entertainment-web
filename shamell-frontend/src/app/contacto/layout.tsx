import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contact & Inquire — Shamell Entertainment",
  description:
    "Begin your Shamell experience. Request a private gala, VIP event, or bespoke collaboration, or let our concierge guide your first step.",
};

export default function ContactoLayout({ children }: { children: React.ReactNode }) {
  return children;
}
