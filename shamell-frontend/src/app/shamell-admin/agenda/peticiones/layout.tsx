import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Inbox — Shamell Admin",
};

export default function PeticionesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
