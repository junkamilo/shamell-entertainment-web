import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Availability — Shamell Admin",
};

export default function DisponibilidadLayout({ children }: { children: React.ReactNode }) {
  return children;
}
