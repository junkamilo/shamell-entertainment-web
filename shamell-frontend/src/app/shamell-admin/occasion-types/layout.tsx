import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Occasion types — Shamell Admin",
};

export default function OccasionTypesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
