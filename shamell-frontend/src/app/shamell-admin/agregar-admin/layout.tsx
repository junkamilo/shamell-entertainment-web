import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Add administrator — Shamell Admin",
};

export default function AgregarAdminLayout({ children }: { children: React.ReactNode }) {
  return children;
}
