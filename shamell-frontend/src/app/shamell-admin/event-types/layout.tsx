import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Event types — Shamell Admin",
};

export default function EventTypesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
