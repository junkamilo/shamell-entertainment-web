import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Gallery categories — Shamell Admin",
};

export default function GalleryCategoriesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
