import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Schedule — Shamell Admin",
};

export default function AgendaLayout({ children }: { children: React.ReactNode }) {
  return children;
}
