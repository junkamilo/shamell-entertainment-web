import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Service types — Shamell Admin",
};

export default function ServiceTypesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
