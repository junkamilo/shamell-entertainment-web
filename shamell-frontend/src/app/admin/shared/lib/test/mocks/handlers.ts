/**
 * `src/app/admin/shared/lib` re-exports `@/lib/admin/*` (auth, routes, pricing, API base).
 * No feature HTTP here. Empty handlers keep the events-style layout; register for consistency.
 */
export const adminSharedLibHandlers: never[] = [];
