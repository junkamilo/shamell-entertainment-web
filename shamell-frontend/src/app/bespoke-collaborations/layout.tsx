import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Bespoke Collaborations — Shamell Entertainment",
  description:
    "For those seeking something truly one-of-a-kind. Shamell collaborates with artists, brands, event designers, and creative directors to craft bespoke performances that push boundaries and tell a story through movement.",
};

export default function BespokeCollaborationsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
